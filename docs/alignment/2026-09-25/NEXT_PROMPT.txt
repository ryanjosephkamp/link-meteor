You are the Link Meteor driver. I am selecting GPT-6 Astra Xhigh for this task. Read /Users/noir/Documents/link-meteor/docs/alignment/2026-09-25/ALIGNMENT.md and preserve its accepted product scope and boundaries. I have reviewed and approve that written alignment. Begin the first autonomous Chrome-extension implementation iteration now.

The only project is /Users/noir/Documents/link-meteor and the only authorized GitHub repository is https://github.com/ryanjosephkamp/link-meteor. Reverify the current files, Git state, remote, and repository instructions. Connect the existing local directory to the repository without overwriting the alignment package or any intervening work. Use a dedicated implementation branch. Commit and push coherent checkpoints periodically and before handback; verify and report remote backup status. Never force-push, rewrite history destructively, or change another repository.

Create the detailed implementation plan and acceptance map from the approved written design, then execute it autonomously. I explicitly choose the Astra-led worker implementation method and authorize you to proceed from planning into implementation without another routine plan-approval exchange. Astra owns architecture, shared integration, independent review, and final acceptance checks. Dispatch GPT-6 Sol High or Xhigh for substantial work and GPT-6 Luna High or Xhigh for bounded, independently verifiable support tasks at your discretion. Use bounded worker agents coordinated in this task. Start with one worker and add a second only for substantial independent work with settled interfaces and disjoint ownership. Maximum two concurrent workers plus Astra; no worker-spawned workers. Record actual dispatch metadata. If an allowed model/effort is unavailable, report the limitation rather than silently substituting another model.

Build the agreed first release: regional drag selection; configurable activation and optional permitted hold-key mode; current-page, selected-tab, window, and all-ordinary-window capture within the current Chrome profile; faithful separate anchor text and URL fields with source provenance; local named collections with notes/tags; review, filtering, sorting, reversible deduplication and undo; Excel .xlsx, CSV, clipboard, Markdown, HTML, JSON and bookmarks; bounded opening of links; useful permission/error/empty states and accessible UI. Follow the complete scope and data contract in ALIGNMENT.md. Keep all features free, with no account, ads, telemetry, backend dependency, or paid tier. Preserve originals and do not fabricate missing anchor text or citation metadata.

Build local deterministic test webpages and test the loaded extension, exported contents, persistence, and permission/partial-failure behavior. Review worker changes yourself, verify the integrated state, and repair or re-dispatch as needed. Clearly distinguish worker reports, automated Chromium checks, installed Chrome checks, and remaining human acceptance. Do not claim compatibility or performance that has not been demonstrated.

Start and record an elapsed wall-clock timer for this iteration. The general limit is about six hours, not a target. Finish earlier if ready. Begin closeout planning near 5.5 hours, avoid launching large new batches then, and choose a natural stopping point around six hours. A short coherent closeout overrun is acceptable; do not silently reset the timer or start another iteration. Before final handback, reconcile all worker writes and safely stop only task-owned work that must end.

Do not delete any files outside /Users/noir/Documents/link-meteor. Do not terminate unrelated Codex tasks, Claude Code sessions, apps, browsers, or servers. Track task-owned processes and browser profiles; use no broad kill commands. Keep new dependencies, profiles, caches, and temporary outputs under the project where configurable. Reuse installed runtimes, keep disk/RAM use modest, bound tabs and traces, and avoid global installs, VMs, containers, or heavyweight downloads. Do not alter account/security settings or request credentials. Keep unexpected resource growth, access blockers, or exhausted capacity visible in a recoverable handback.

Do not implement Firefox/Safari ports, AI features, or the GitHub Pages site in this iteration. Do not launch Claude Code, publish the extension to the Chrome Web Store, make payments, deploy the site, or schedule further background iterations. The user will have Claude Opus 5.5 refine extension aesthetics/UI/UX and build/design the GitHub Pages site after a coherent working Chrome baseline exists.

When that baseline is ready, prepare a self-contained Opus handoff package with source/commit identity, artifacts, implemented screenshots, functional requirements, test evidence, limitations, separate extension/site briefs, and a return-to-Astra prompt. Tell Opus to inspect the project and create its own scaffolding before beginning. Give context and outcomes without prescribing its technology stack, governance, workflow, or scaffolding; Opus uses its own judgment while preserving the user-facing contract and project boundaries. The user will initiate that work and later return for Astra's final functionality audit.

At completion, a material blocker, or the time-limit closeout, use /Users/noir/.codex/skills/render-mobile-html-report/SKILL.md to produce a standalone mobile-readable HTML handback with canonical Markdown and one exact embedded next prompt. Include screenshots/build artifacts where relevant, actual checks and limits, elapsed time, branch/commit/push status, and remaining work. Assume Copy does not work in mobile preview and do not investigate that limitation. Keep the prompt fully readable. Stop after the handback for my review.
